{
  "name": "@aviaframe/portal",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "echo \"Building portal...\" && mkdir -p dist && cp -R src/* dist/ || true",
    "test": "jest --runInBand",
    "lint": "eslint src tests"
  },
  "dependencies": {
    "express": "^4.19.2",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {}
}