{
  "name": "@aviaframe/backend",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "echo \"Building backend...\" && mkdir -p dist && cp -R src/* dist/ || true",
    "test": "jest --runInBand",
    "lint": "eslint src tests"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "supertest": "^6.4.0"
  }
}