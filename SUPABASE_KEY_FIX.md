# 🔑 Исправление Supabase API Key (401 Unauthorized)

## ❌ Ошибка

```
POST https://kirvqjgyxjyvwflghchw.supabase.co/auth/v1/otp 401 (Unauthorized)
AuthApiError: Invalid API key
```

**Причина**: API ключ в `.env` невалидный, устарел или был регенерирован.

---

## ✅ Решение (5 минут)

### Шаг 1: Откройте Supabase Dashboard

Перейдите по ссылке:
```
https://supabase.com/dashboard/project/kirvqjgyxjyvwflghchw/settings/api
```

**Если страница не открывается** ("Project not found"):
- Проект был удален
- Нужно создать новый проект (см. ниже раздел "Создание нового проекта")

### Шаг 2: Скопируйте правильные ключи

На странице API Settings найдите 2 ключа:

#### 1. Project URL
```
https://kirvqjgyxjyvwflghchw.supabase.co
```
*(должен быть таким же)*

#### 2. anon / public key
Длинный JWT токен, начинается с `eyJhbG...`

**Пример правильного ключа:**
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImtpcnZxamd5eGp5dndmbGdoY2h3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3Mzg1MDI4NzUsImV4cCI6MjA1NDA3ODg3NX0.sEVKR2Lm5K6rZqOqtG8_QQN1xIcYl0vwC5pRaEg-sKg
```

**НЕ копируйте** ключ из раздела "service_role" - он секретный!

### Шаг 3: Обновите .env файл

Откройте файл:
```
/Users/sergejdaniluk/projects/aviaframe/portal/client/.env
```

Замените строку:
```bash
# СТАРЫЙ КЛЮЧ (если он не работает)
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImtpcnZxamd5eGp5dndmbGdoY2h3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3Mzg1MDI4NzUsImV4cCI6MjA1NDA3ODg3NX0.sEVKR2Lm5K6rZqOqtG8_QQN1xIcYl0vwC5pRaEg-sKg

# НОВЫЙ КЛЮЧ (скопируйте из Dashboard)
VITE_SUPABASE_ANON_KEY=<ваш новый ключ из Supabase Dashboard>
```

### Шаг 4: Перезапустите dev server

**ВАЖНО!** `.env` читается только при старте сервера.

```bash
# Остановите текущий сервер (Ctrl+C)
cd /Users/sergejdaniluk/projects/aviaframe/portal/client

# Перезапустите
npm run dev
```

### Шаг 5: Проверьте Console

Откройте http://localhost:3002

Нажмите F12 → Console

Должно быть:
```
✅ Supabase initialized: {
  url: "https://kirvqjgyxjyvwflghchw.supabase.co",
  keyPrefix: "eyJhbGciOiJIUzI1NiI...",
  keyLength: 219
}
```

Если видите `❌ Supabase credentials missing` - значит ключ не загрузился.

### Шаг 6: Попробуйте снова

1. Нажмите **"Sign In"**
2. Введите email
3. Нажмите **"Continue with Email"**

Теперь должно сработать без ошибки 401.

---

## 🆘 Если проект не открывается

### Вариант 1: Проверьте другие проекты

Откройте:
```
https://supabase.com/dashboard/projects
```

Посмотрите список всех ваших проектов. Возможно, вы использовали другой проект.

### Вариант 2: Создайте новый проект

Если старый проект был удален, создайте новый:

1. Перейдите: https://supabase.com/dashboard/projects
2. Нажмите **"New Project"**
3. Заполните:
   - **Name**: Aviaframe
   - **Database Password**: (сохраните!)
   - **Region**: Closest to you
4. Нажмите **"Create new project"**

**Подождите 2-3 минуты** пока проект создается.

### Вариант 3: Запустите SQL schema

После создания нового проекта:

1. Перейдите в **SQL Editor**
2. Скопируйте содержимое файла:
   ```
   /Users/sergejdaniluk/projects/aviaframe/backend/supabase/schema.sql
   ```
3. Вставьте в SQL Editor
4. Нажмите **"Run"**

Это создаст все таблицы (agencies, profiles, orders, passengers и т.д.)

### Вариант 4: Обновите .env с новым проектом

После создания нового проекта:

1. Перейдите в **Settings** → **API**
2. Скопируйте:
   - **Project URL** (новый!)
   - **anon public key** (новый!)
3. Обновите `.env`:

```bash
VITE_SUPABASE_URL=https://your-new-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

4. Перезапустите `npm run dev`

---

## 🔍 Диагностика проблем

### Проверка 1: Правильный ли URL?

```bash
# Должен вернуть JSON с information_schema
curl https://kirvqjgyxjyvwflghchw.supabase.co/rest/v1/
```

Если `404 Not Found` - проект не существует.

### Проверка 2: Правильный ли ключ?

```bash
# Замените YOUR_KEY на ваш ключ из .env
curl -H "apikey: YOUR_KEY" \
  https://kirvqjgyxjyvwflghchw.supabase.co/rest/v1/
```

Если `401 Unauthorized` - ключ неверный.

Если `200 OK` - ключ правильный, но возможно не загрузился в приложение.

### Проверка 3: Загрузился ли ключ в приложение?

В Console (F12) введите:
```javascript
console.log(import.meta.env.VITE_SUPABASE_ANON_KEY);
```

Должен вывести ваш ключ. Если `undefined` - не загрузился.

**Решение**: Перезапустите `npm run dev`

---

## 📋 Checklist

После исправления проверьте:

- [ ] Supabase Dashboard открывается
- [ ] API ключ скопирован правильно (начинается с `eyJ`)
- [ ] `.env` файл обновлен с новым ключом
- [ ] Dev server перезапущен (`Ctrl+C` → `npm run dev`)
- [ ] Console показывает `✅ Supabase initialized`
- [ ] Нет ошибок в Console
- [ ] Попытка Sign In не выдает 401 ошибку
- [ ] Письмо с OTP приходит на email

---

## 🎯 Быстрый тест после исправления

```bash
# Терминал 1: Убедитесь что сервер запущен
cd /Users/sergejdaniluk/projects/aviaframe/portal/client
npm run dev

# Терминал 2: Проверьте что ключ работает
curl -H "apikey: YOUR_ANON_KEY" \
  https://kirvqjgyxjyvwflghchw.supabase.co/auth/v1/settings
```

Должно вернуть JSON с настройками auth (не 401).

---

## 📧 Следующий шаг

После исправления ключа:

1. Попробуйте войти снова
2. Должно прийти письмо с OTP
3. Проверьте Spam folder
4. Введите 6-значный код

Если письмо не приходит - см. [SUPABASE_AUTH_SETUP.md](SUPABASE_AUTH_SETUP.md)

---

**Статус**: Исправьте ключ, затем тестируйте снова! 🔑
